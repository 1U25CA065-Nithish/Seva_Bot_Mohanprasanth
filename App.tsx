import { type ReactNode, useEffect, useState } from 'react';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';
import {
  Activity, AlertTriangle, ArrowRight, BarChart3, Bell, BriefcaseBusiness, Building2,
  CalendarDays, Check, CheckCircle2, ChevronDown, ChevronRight, CircleHelp, ClipboardCheck,
  Clock3, CloudUpload, Download, ExternalLink, FileCheck2, FileText, Filter, FolderOpen,
  Gauge, Gavel, Info, LayoutDashboard, ListFilter, LockKeyhole, LogIn, Menu,
  MessageSquareText, MoreHorizontal, Plus, RefreshCw, Search, Settings, ShieldCheck, Sparkles,
  Target, TrendingUp, Upload, UsersRound, X,
} from 'lucide-react';
import {
  Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer,
  Tooltip as ChartTooltip, XAxis, YAxis,
} from 'recharts';
import {
  Link, Route, Switch, useLocation, useParams, Router as WouterRouter,
} from 'wouter';

const STORAGE_PREFIX = 'seva-bot:';

function readLocal<T>(key: string, fallback: T): T {
  if (typeof window === 'undefined') return fallback;
  try {
    const stored = window.localStorage.getItem(`${STORAGE_PREFIX}${key}`);
    return stored === null ? fallback : (JSON.parse(stored) as T);
  } catch {
    return fallback;
  }
}

function usePersistentState<T>(key: string, initialValue: T) {
  const [value, setValue] = useState<T>(() => readLocal(key, initialValue));

  useEffect(() => {
    try {
      window.localStorage.setItem(`${STORAGE_PREFIX}${key}`, JSON.stringify(value));
    } catch {
      // The app remains usable if storage is unavailable or full.
    }
  }, [key, value]);

  return [value, setValue] as const;
}

type AssistantMessage = { role: 'ai' | 'you'; text: string };

type IconType = typeof Activity;
type Tone = 'navy' | 'amber' | 'teal' | 'red' | 'slate';

const navGroups = [
  { label: 'Workspace', items: [
    { href: '/dashboard', label: 'Overview', icon: LayoutDashboard },
    { href: '/projects', label: 'Projects', icon: BriefcaseBusiness },
    { href: '/approvals', label: 'Approvals', icon: Gavel },
    { href: '/applications', label: 'Applications', icon: ClipboardCheck },
  ]},
  { label: 'Operations', items: [
    { href: '/documents', label: 'Documents', icon: FolderOpen },
    { href: '/inspections', label: 'Inspections', icon: CalendarDays },
    { href: '/renewals', label: 'Renewals', icon: RefreshCw },
    { href: '/incentives', label: 'Incentives', icon: Target },
    { href: '/compliance', label: 'Compliance', icon: ShieldCheck },
  ]},
  { label: 'Support', items: [
    { href: '/grievances', label: 'Grievances', icon: MessageSquareText },
    { href: '/notifications', label: 'Notifications', icon: Bell, badge: 3 },
  ]},
];

const approvalData = [
  { id: 'pollution-consent', name: 'Consent to Establish', agency: 'Tamil Nadu Pollution Control Board', domain: 'Environment', status: 'Action needed', due: '18 Jun 2025', progress: 72, tone: 'amber' as Tone, initials: 'TNPCB' },
  { id: 'factory-license', name: 'Factory Plan Approval', agency: 'Directorate of Industrial Safety & Health', domain: 'Safety', status: 'Under review', due: '24 Jun 2025', progress: 46, tone: 'navy' as Tone, initials: 'DISH' },
  { id: 'building-permit', name: 'Building Plan Sanction', agency: 'Coimbatore City Municipal Corporation', domain: 'Construction', status: 'Approved', due: 'Completed 03 May', progress: 100, tone: 'teal' as Tone, initials: 'CCMC' },
  { id: 'power-connection', name: 'High Tension Power Connection', agency: 'Tamil Nadu Generation & Distribution Corporation', domain: 'Utilities', status: 'Documents requested', due: '02 Jul 2025', progress: 58, tone: 'red' as Tone, initials: 'TANGEDCO' },
  { id: 'fire-noc', name: 'Fire Safety NOC', agency: 'Tamil Nadu Fire & Rescue Services', domain: 'Safety', status: 'Approved', due: 'Completed 28 Apr', progress: 100, tone: 'teal' as Tone, initials: 'TNFRS' },
];

const documentData = [
  { id: 'doc-01', name: 'Land ownership deed', type: 'Legal', date: '12 May 2025', size: '2.4 MB', status: 'Verified', icon: FileCheck2 },
  { id: 'doc-02', name: 'Factory layout — Rev 03', type: 'Technical', date: '08 May 2025', size: '8.7 MB', status: 'Verified', icon: FileText },
  { id: 'doc-03', name: 'Environmental management plan', type: 'Environment', date: '03 May 2025', size: '4.1 MB', status: 'Needs update', icon: FileText },
  { id: 'doc-04', name: 'Partnership deed', type: 'Company', date: '18 Apr 2025', size: '1.2 MB', status: 'Verified', icon: FileCheck2 },
  { id: 'doc-05', name: 'Fire equipment certificate', type: 'Safety', date: '28 Apr 2025', size: '830 KB', status: 'Verified', icon: FileCheck2 },
  { id: 'doc-06', name: 'Water source declaration', type: 'Utilities', date: '21 Apr 2025', size: '640 KB', status: 'Expiring soon', icon: Clock3 },
];

const applications = [
  { id: 'APP-24091', name: 'Consent to Establish', agency: 'TNPCB', submitted: '02 Jun 2025', status: 'Action needed', updated: 'Today', risk: 'Medium' },
  { id: 'APP-24074', name: 'Factory Plan Approval', agency: 'DISH', submitted: '26 May 2025', status: 'Under review', updated: 'Yesterday', risk: 'Low' },
  { id: 'APP-23988', name: 'Building Plan Sanction', agency: 'CCMC', submitted: '14 Apr 2025', status: 'Approved', updated: '03 May', risk: 'Low' },
  { id: 'APP-23845', name: 'High Tension Power Connection', agency: 'TANGEDCO', submitted: '30 Mar 2025', status: 'Documents requested', updated: '28 May', risk: 'High' },
  { id: 'APP-23812', name: 'Fire Safety NOC', agency: 'TNFRS', submitted: '21 Mar 2025', status: 'Approved', updated: '28 Apr', risk: 'Low' },
];

const notifyData = [
  { id: 'n1', title: 'Upload the revised environmental management plan', body: 'TNPCB has requested one updated document before scrutiny can continue.', time: '18 min ago', tone: 'amber', unread: true, link: '/approvals/pollution-consent' },
  { id: 'n2', title: 'Inspection scheduled for your facility', body: 'Joint inspection is confirmed for 20 June 2025, 10:30 AM.', time: 'Yesterday', tone: 'navy', unread: true, link: '/inspections' },
  { id: 'n3', title: 'Building plan sanction approved', body: 'Your approval certificate is ready to download.', time: '03 May', tone: 'teal', unread: true, link: '/applications' },
  { id: 'n4', title: 'Water source declaration expires in 19 days', body: 'Renew early to keep your utility approvals in good standing.', time: '06 Jun', tone: 'red', unread: false, link: '/renewals' },
];

function cn(...classes: Array<string | false | null | undefined>) { return classes.filter(Boolean).join(' '); }

function Logo({ dark = false }: { dark?: boolean }) {
  return <Link href="/dashboard" data-testid="link-brand" className="flex items-center gap-3">
    <span className={cn('grid h-9 w-9 place-items-center rounded-xl font-display text-sm font-bold shadow-sm', dark ? 'bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]' : 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]')}>SB</span>
    <span className={cn('font-display text-[17px] font-bold tracking-tight', dark ? 'text-white' : 'text-[hsl(var(--foreground))]')}>Seva <span className={dark ? 'text-[hsl(var(--accent))]' : 'text-[hsl(var(--primary))]'}>bot</span></span>
  </Link>;
}

function Badge({ children, tone = 'slate' }: { children: ReactNode; tone?: Tone }) {
  const styles = { navy: 'bg-[hsl(var(--primary)/.09)] text-[hsl(var(--primary))]', amber: 'bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]', teal: 'bg-[hsl(var(--chart-3)/.14)] text-[hsl(165_48%_29%)]', red: 'bg-[hsl(var(--destructive)/.11)] text-[hsl(var(--destructive))]', slate: 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))]' };
  return <span className={cn('inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-semibold', styles[tone])}>{children}</span>;
}

function Button({ children, variant = 'primary', onClick, className, disabled = false, testId, type = 'button' }: { children: ReactNode; variant?: 'primary' | 'outline' | 'ghost' | 'danger'; onClick?: () => void; className?: string; disabled?: boolean; testId?: string; type?: 'button' | 'submit' }) {
  const styles = { primary: 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))] hover:opacity-90', outline: 'border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-[hsl(var(--foreground))] hover:bg-[hsl(var(--muted))]', ghost: 'text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))] hover:text-[hsl(var(--foreground))]', danger: 'bg-[hsl(var(--destructive))] text-white hover:opacity-90' };
  return <button type={type} disabled={disabled} onClick={onClick} data-testid={testId} className={cn('inline-flex min-h-9 items-center justify-center gap-2 rounded-lg px-3.5 text-sm font-semibold disabled:cursor-not-allowed disabled:opacity-50', styles[variant], className)}>{children}</button>;
}

function PageHeader({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode }) {
  return <div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end">
    <div><div className="cg-kicker mb-2">{eyebrow ?? 'Seva bot workspace'}</div><h1 className="font-display text-2xl font-bold tracking-tight text-[hsl(var(--foreground))] md:text-[28px]">{title}</h1>{description && <p className="mt-1.5 max-w-2xl text-sm text-[hsl(var(--muted-foreground))]">{description}</p>}</div>
    {actions && <div className="flex flex-wrap gap-2">{actions}</div>}
  </div>;
}

function StatCard({ label, value, helper, icon: Icon, tone = 'navy', action }: { label: string; value: string; helper: string; icon: IconType; tone?: Tone; action?: () => void }) {
  const accent = { navy: 'bg-[hsl(var(--primary))]', amber: 'bg-[hsl(var(--accent))]', teal: 'bg-[hsl(var(--chart-3))]', red: 'bg-[hsl(var(--destructive))]', slate: 'bg-[hsl(var(--muted-foreground))]' };
  return <button onClick={action} data-testid={`stat-${label.toLowerCase().replaceAll(' ', '-')}`} className="group relative overflow-hidden rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4 text-left shadow-[0_2px_8px_hsl(var(--primary)/.035)] hover:-translate-y-0.5 hover:shadow-[0_8px_20px_hsl(var(--primary)/.08)]">
    <div className={cn('absolute inset-x-0 top-0 h-0.5', accent[tone])} /><div className="mb-5 flex items-start justify-between"><span className="text-xs font-medium text-[hsl(var(--muted-foreground))]">{label}</span><span className={cn('grid h-8 w-8 place-items-center rounded-lg text-white', accent[tone])}><Icon size={15} /></span></div><div className="cg-number text-2xl font-semibold text-[hsl(var(--foreground))]">{value}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{helper}</div>
  </button>;
}

function Shell({ children }: { children: ReactNode }) {
  const [location] = useLocation();
  const [mobileOpen, setMobileOpen] = useState(false);
  const [assistant, setAssistant] = useState(false);
  const [searchOpen, setSearchOpen] = useState(false);
  const [toast, setToast] = useState('');
  const [offline, setOffline] = useState(() => typeof navigator !== 'undefined' && !navigator.onLine);
  useEffect(() => {
    const handleOnline = () => setOffline(false);
    const handleOffline = () => setOffline(true);
    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);
    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, []);
  const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2800); };
  const officer = location.startsWith('/department');
  const nav = officer ? [
    { href: '/department', label: 'Operations overview', icon: Gauge },
    { href: '/department/applications/APP-24091', label: 'Review queue', icon: ClipboardCheck },
    { href: '/analytics', label: 'Analytics', icon: BarChart3 },
    { href: '/notifications', label: 'Notifications', icon: Bell, badge: 3 },
  ] : navGroups.flatMap(group => group.items);
  return <div className="min-h-[100dvh] bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
    <aside className="fixed inset-y-0 left-0 z-30 hidden w-[238px] flex-col bg-[hsl(var(--sidebar))] px-3 py-5 text-[hsl(var(--sidebar-foreground))] md:flex">
      <div className="px-3 pb-8"><Logo dark /></div>
      <div className="mb-3 px-3 cg-kicker text-[hsl(var(--sidebar-foreground)/.48)]">{officer ? 'Officer console' : 'Applicant workspace'}</div>
      <nav className="cg-scrollbar flex-1 space-y-1 overflow-y-auto">{officer ? <NavItems items={nav} location={location} /> : navGroups.map(group => <div key={group.label} className="mb-6"><div className="mb-2 px-3 text-[10px] font-semibold uppercase tracking-[.14em] text-[hsl(var(--sidebar-foreground)/.42)]">{group.label}</div><NavItems items={group.items} location={location} /></div>)}</nav>
      <div className="mt-4 border-t border-[hsl(var(--sidebar-border))] pt-4">
        <button onClick={() => showToast('Help centre opened')} data-testid="button-help" className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-[hsl(var(--sidebar-foreground)/.65)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-white"><CircleHelp size={16} />Help centre</button>
        <Link href="/settings" data-testid="link-settings" className="mt-1 flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-[hsl(var(--sidebar-foreground)/.65)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-white"><Settings size={16} />Settings</Link>
        <div className="mt-5 flex items-center gap-3 rounded-xl bg-[hsl(var(--sidebar-accent))] p-3"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-[hsl(var(--accent))] text-xs font-bold text-[hsl(var(--accent-foreground))]">QT</span><span className="min-w-0"><span className="block truncate text-xs font-semibold text-white">Quantum Thinkers</span><span className="block truncate text-[10px] text-[hsl(var(--sidebar-foreground)/.6)]">Founder · GreenTech</span></span><ChevronDown size={14} className="ml-auto text-[hsl(var(--sidebar-foreground)/.5)]" /></div>
      </div>
    </aside>
    {mobileOpen && <div className="fixed inset-0 z-40 bg-[hsl(var(--primary)/.35)] md:hidden" onClick={() => setMobileOpen(false)}><aside className="h-full w-[276px] bg-[hsl(var(--sidebar))] p-4 text-white" onClick={e => e.stopPropagation()}><div className="flex items-center justify-between px-2 pb-7"><Logo dark /><button onClick={() => setMobileOpen(false)} data-testid="button-close-mobile" className="text-white/70"><X size={20} /></button></div><nav className="space-y-1"><NavItems items={nav} location={location} onNavigate={() => setMobileOpen(false)} /></nav></aside></div>}
     <div className="md:pl-[238px]">
       {offline && <div role="status" data-testid="offline-status" className="flex items-center justify-center gap-2 bg-[hsl(var(--accent))] px-4 py-2 text-xs font-semibold text-[hsl(var(--accent-foreground))]"><span className="h-1.5 w-1.5 rounded-full bg-current" />Offline mode · Your changes are saved on this device</div>}
      <header className="sticky top-0 z-20 flex h-[68px] items-center justify-between border-b border-[hsl(var(--border))] bg-[hsl(var(--background)/.93)] px-4 backdrop-blur-md md:px-8">
        <div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} data-testid="button-open-mobile" className="rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))] md:hidden"><Menu size={20} /></button><div className="hidden items-center gap-2 text-xs text-[hsl(var(--muted-foreground))] sm:flex"><span>Workspace</span><ChevronRight size={13} /><span className="font-semibold text-[hsl(var(--foreground))]">{officer ? 'Officer console' : location === '/dashboard' ? 'Overview' : location.split('/')[1]?.replace('-', ' ')}</span></div><div className="sm:hidden"><Logo /></div></div>
        <div className="flex items-center gap-1.5 md:gap-3"><button onClick={() => setSearchOpen(true)} data-testid="button-global-search" className="flex items-center gap-2 rounded-lg border border-transparent px-2.5 py-2 text-[hsl(var(--muted-foreground))] hover:border-[hsl(var(--border))] hover:bg-[hsl(var(--card))]"><Search size={17} /><span className="hidden text-xs sm:block">Search anything</span><kbd className="hidden rounded bg-[hsl(var(--muted))] px-1.5 py-0.5 text-[10px] sm:block">⌘ K</kbd></button><Link href="/notifications" data-testid="link-notifications" className="relative rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Bell size={18} /><span className="absolute right-1 top-1 h-1.5 w-1.5 rounded-full bg-[hsl(var(--accent))]" /></Link><button onClick={() => setAssistant(true)} data-testid="button-open-assistant" className="hidden items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 py-2 text-xs font-semibold hover:bg-[hsl(var(--muted))] sm:flex"><Sparkles size={14} className="text-[hsl(var(--accent-foreground))]" /><span>Ask Seva bot</span></button></div>
      </header>
      <main className="mx-auto max-w-[1440px] p-4 md:p-8">{children}</main>
    </div>
    {searchOpen && <SearchModal onClose={() => setSearchOpen(false)} />}
    {assistant && <Assistant onClose={() => setAssistant(false)} />}
    {toast && <div role="status" data-testid="toast-message" className="fixed bottom-5 left-1/2 z-[60] -translate-x-1/2 rounded-xl bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-white shadow-xl">{toast}</div>}
  </div>;
}

function NavItems({ items, location, onNavigate }: { items: Array<{ href: string; label: string; icon: IconType; badge?: number }>; location: string; onNavigate?: () => void }) {
  return <>{items.map(item => { const active = location === item.href || (item.href !== '/dashboard' && location.startsWith(item.href)); const Icon = item.icon; return <Link key={item.href} href={item.href} onClick={onNavigate} data-testid={`link-nav-${item.label.toLowerCase().replaceAll(' ', '-')}`} className={cn('flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm', active ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-white shadow-sm' : 'text-[hsl(var(--sidebar-foreground)/.64)] hover:bg-[hsl(var(--sidebar-accent)/.7)] hover:text-white')}><Icon size={16} strokeWidth={active ? 2.2 : 1.8} /><span className="flex-1">{item.label}</span>{item.badge && <span className="grid h-5 min-w-5 place-items-center rounded-full bg-[hsl(var(--accent))] px-1 text-[10px] font-bold text-[hsl(var(--accent-foreground))]">{item.badge}</span>}</Link>; })}</>;
}

function SearchModal({ onClose }: { onClose: () => void }) {
  const [term, setTerm] = useState('');
  const results = [...approvalData.map(a => ({ label: a.name, meta: a.agency, href: `/approvals/${a.id}`, icon: Gavel })), ...documentData.map(d => ({ label: d.name, meta: d.type, href: '/documents', icon: FileText }))].filter(item => `${item.label} ${item.meta}`.toLowerCase().includes(term.toLowerCase()));
  return <div className="fixed inset-0 z-50 bg-[hsl(var(--primary)/.3)] p-4 pt-[12vh]" onClick={onClose}><div className="mx-auto max-w-xl overflow-hidden rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] shadow-2xl" onClick={e => e.stopPropagation()}><div className="flex items-center gap-3 border-b border-[hsl(var(--border))] px-4"><Search size={18} className="text-[hsl(var(--muted-foreground))]" /><input autoFocus value={term} onChange={e => setTerm(e.target.value)} data-testid="input-global-search" placeholder="Search approvals, documents, applications..." className="h-14 flex-1 bg-transparent text-sm outline-none" /><button onClick={onClose} data-testid="button-close-search"><X size={18} className="text-[hsl(var(--muted-foreground))]" /></button></div><div className="max-h-[380px] overflow-y-auto p-2">{results.length ? results.map(item => { const Icon = item.icon; return <Link key={item.label} href={item.href} onClick={onClose} data-testid={`search-result-${item.label.toLowerCase().replaceAll(' ', '-')}`} className="flex items-center gap-3 rounded-xl p-3 hover:bg-[hsl(var(--muted))]"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><Icon size={16} /></span><span><span className="block text-sm font-semibold">{item.label}</span><span className="block text-xs text-[hsl(var(--muted-foreground))]">{item.meta}</span></span><ArrowRight size={15} className="ml-auto text-[hsl(var(--muted-foreground))]" /></Link>; }) : <div className="p-8 text-center text-sm text-[hsl(var(--muted-foreground))]">No matching records. Try another term.</div>}</div></div></div>;
}

function Assistant({ onClose }: { onClose: () => void }) {
  const [question, setQuestion] = useState('');
  const [messages, setMessages] = usePersistentState<AssistantMessage[]>('assistant-messages', [{ role: 'ai', text: 'Good morning, Quantum Thinkers. I can help you understand approvals, documents, or what needs attention next.' }]);
  const ask = () => { if (!question.trim()) return; const q = question.trim(); setMessages(prev => [...prev, { role: 'you', text: q }, { role: 'ai', text: q.toLowerCase().includes('tnpcb') || q.toLowerCase().includes('environment') ? 'Your TNPCB consent is waiting on the revised environmental management plan. Upload it from the approval detail to move scrutiny forward.' : q.toLowerCase().includes('inspection') ? 'The joint inspection is scheduled for 20 June at 10:30 AM. Keep the layout revision and fire equipment certificate ready.' : 'I found this in your workspace: 3 items need attention this week. I can take you to the highest-impact one if you like.' }]); setQuestion(''); };
  return <div className="fixed inset-0 z-50" onClick={onClose}><div className="absolute inset-0 bg-[hsl(var(--primary)/.2)]" /><aside className="absolute right-0 top-0 flex h-full w-full max-w-[410px] flex-col border-l border-[hsl(var(--border))] bg-[hsl(var(--card))] shadow-2xl" onClick={e => e.stopPropagation()}><div className="flex items-center justify-between border-b border-[hsl(var(--border))] p-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]"><Sparkles size={17} /></span><span><span className="block font-semibold">Ask Seva bot</span><span className="block text-xs text-[hsl(var(--muted-foreground))]">Your approvals co-pilot</span></span></div><button onClick={onClose} data-testid="button-close-assistant"><X size={19} /></button></div><div className="cg-scrollbar flex-1 space-y-4 overflow-y-auto p-5">{messages.map((message, i) => <div key={`${message.role}-${i}`} className={cn('flex', message.role === 'you' ? 'justify-end' : 'justify-start')}><div className={cn('max-w-[86%] rounded-2xl px-4 py-3 text-sm leading-relaxed', message.role === 'you' ? 'rounded-br-sm bg-[hsl(var(--primary))] text-white' : 'rounded-bl-sm bg-[hsl(var(--muted))] text-[hsl(var(--foreground))]')} data-testid={`assistant-message-${i}`}>{message.text}</div></div>)}</div><div className="border-t border-[hsl(var(--border))] p-4"><div className="mb-3 flex flex-wrap gap-2"><button onClick={() => setQuestion('What should I prioritise?')} data-testid="assistant-suggestion-priority" className="rounded-full border border-[hsl(var(--border))] px-3 py-1.5 text-[11px] text-[hsl(var(--muted-foreground))]">What should I prioritise?</button><button onClick={() => setQuestion('Tell me about my inspection')} data-testid="assistant-suggestion-inspection" className="rounded-full border border-[hsl(var(--border))] px-3 py-1.5 text-[11px] text-[hsl(var(--muted-foreground))]">My inspection</button></div><div className="flex gap-2 rounded-xl border border-[hsl(var(--border))] p-2"><input value={question} onChange={e => setQuestion(e.target.value)} onKeyDown={e => e.key === 'Enter' && ask()} data-testid="input-assistant" placeholder="Ask about your workspace..." className="min-w-0 flex-1 bg-transparent px-2 text-sm outline-none" /><Button onClick={ask} testId="button-send-assistant" className="h-9 w-9 p-0"><ArrowRight size={16} /></Button></div></div></aside></div>;
}

function Dashboard() {
  const [, setLocation] = useLocation();
  return <><PageHeader eyebrow="Monday · 09 June 2025" title="Good morning, Quantum Thinkers." description="Here is the current pulse of GreenTech Manufacturing Unit." actions={<><Button variant="outline" onClick={() => setLocation('/documents')} testId="button-upload-document"><Upload size={15} />Upload document</Button><Button onClick={() => setLocation('/approvals')} testId="button-view-roadmap">View roadmap <ArrowRight size={15} /></Button></>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Project progress" value="68%" helper="On track · target 74% by July" icon={TrendingUp} tone="teal" /><StatCard label="Active approvals" value="7" helper="2 need your attention" icon={Gavel} tone="amber" action={() => setLocation('/approvals')} /><StatCard label="Compliance health" value="86 / 100" helper="Good standing · 1 alert" icon={ShieldCheck} tone="teal" action={() => setLocation('/compliance')} /><StatCard label="Next deadline" value="18 Jun" helper="TNPCB document upload" icon={Clock3} tone="red" action={() => setLocation('/approvals/pollution-consent')} /></div><div className="mt-6 grid gap-5 xl:grid-cols-[1.4fr_.9fr]"><div className="space-y-5"><ProjectProgress /><AttentionPanel /><RecentActivity /></div><div className="space-y-5"><ProjectCard /><UpcomingCard /><IncentiveCallout /></div></div></>;
}

function ProjectProgress() {
  return <section className="cg-rise rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-start justify-between"><div><div className="cg-kicker mb-2">Live project pulse</div><h2 className="font-display text-lg font-bold">GreenTech Manufacturing Unit</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Coimbatore · Tamil Nadu · Food processing</p></div><Badge tone="teal"><span className="h-1.5 w-1.5 rounded-full bg-current" />On track</Badge></div><div className="mt-8 flex items-end justify-between"><div><span className="cg-number text-5xl font-semibold tracking-tight">68</span><span className="ml-1 text-xl text-[hsl(var(--muted-foreground))]">%</span></div><span className="text-xs text-[hsl(var(--muted-foreground))]">₹8.5 Crore investment</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-[hsl(var(--muted))]"><div className="h-full rounded-full bg-[hsl(var(--chart-3))]" style={{ width: '68%' }} /></div><div className="mt-3 flex justify-between text-xs text-[hsl(var(--muted-foreground))]"><span>Started Jan 2025</span><span>Expected Dec 2025</span></div><div className="mt-7 grid grid-cols-3 gap-3 border-t border-[hsl(var(--border))] pt-4 text-center"><div><div className="cg-number text-lg font-semibold">12</div><div className="text-[11px] text-[hsl(var(--muted-foreground))]">Approvals</div></div><div><div className="cg-number text-lg font-semibold">34</div><div className="text-[11px] text-[hsl(var(--muted-foreground))]">Documents</div></div><div><div className="cg-number text-lg font-semibold">02</div><div className="text-[11px] text-[hsl(var(--muted-foreground))]">Open alerts</div></div></div></section>;
}

function AttentionPanel() {
  const [, setLocation] = useLocation();
  return <section className="rounded-xl border border-[hsl(var(--accent)/.55)] bg-[hsl(var(--accent)/.08)] p-5"><div className="flex items-start gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-lg bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]"><AlertTriangle size={17} /></span><div><h2 className="font-semibold">Two things need your attention</h2><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">Keeping these moving protects your June timeline.</p></div></div><div className="mt-5 divide-y divide-[hsl(var(--accent)/.22)]"><button onClick={() => setLocation('/approvals/pollution-consent')} data-testid="attention-approval" className="flex w-full items-center gap-3 py-3 text-left"><span className="grid h-7 w-7 place-items-center rounded-full bg-[hsl(var(--card))] text-[hsl(var(--destructive))]"><FileText size={14} /></span><span className="flex-1"><span className="block text-sm font-semibold">Upload revised environmental plan</span><span className="block text-xs text-[hsl(var(--muted-foreground))]">TNPCB · due 18 June</span></span><ChevronRight size={16} /></button><button onClick={() => setLocation('/inspections')} data-testid="attention-inspection" className="flex w-full items-center gap-3 py-3 text-left"><span className="grid h-7 w-7 place-items-center rounded-full bg-[hsl(var(--card))] text-[hsl(var(--primary))]"><CalendarDays size={14} /></span><span className="flex-1"><span className="block text-sm font-semibold">Prepare for joint inspection</span><span className="block text-xs text-[hsl(var(--muted-foreground))]">20 June · 10:30 AM</span></span><ChevronRight size={16} /></button></div></section>;
}

function RecentActivity() {
  const items = [{ text: 'Building Plan Sanction approved', time: '03 May 2025', icon: CheckCircle2, tone: 'teal' as Tone }, { text: 'Factory layout Rev 03 uploaded', time: '08 May 2025', icon: Upload, tone: 'navy' as Tone }, { text: 'TNPCB requested one document', time: 'Today, 08:42 AM', icon: AlertTriangle, tone: 'amber' as Tone }];
  return <section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="mb-4 flex items-center justify-between"><h2 className="font-display text-base font-bold">Recent activity</h2><Link href="/notifications" data-testid="link-view-activity" className="text-xs font-semibold text-[hsl(var(--primary))]">View all</Link></div><div className="space-y-4">{items.map((item, i) => { const Icon = item.icon; return <div className="flex gap-3" key={item.text}><span className={cn('mt-0.5 grid h-8 w-8 shrink-0 place-items-center rounded-full', item.tone === 'teal' ? 'bg-[hsl(var(--chart-3)/.13)] text-[hsl(var(--chart-3))]' : item.tone === 'amber' ? 'bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]' : 'bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]')}><Icon size={15} /></span><div><p className="text-sm font-medium">{item.text}</p><p className="mt-0.5 text-xs text-[hsl(var(--muted-foreground))]">{item.time}</p></div>{i === 2 && <Badge tone="amber">New</Badge>}</div>; })}</div></section>;
}

function ProjectCard() {
  return <section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><h2 className="font-display text-base font-bold">Project profile</h2><Link href="/projects" data-testid="link-project-profile" className="text-xs font-semibold text-[hsl(var(--primary))]">Open profile</Link></div><div className="mt-5 grid grid-cols-2 gap-y-4 text-sm"><div><div className="text-xs text-[hsl(var(--muted-foreground))]">Entity type</div><div className="mt-1 font-medium">Private limited</div></div><div><div className="text-xs text-[hsl(var(--muted-foreground))]">Sector</div><div className="mt-1 font-medium">Food processing</div></div><div><div className="text-xs text-[hsl(var(--muted-foreground))]">Location</div><div className="mt-1 font-medium">Coimbatore, TN</div></div><div><div className="text-xs text-[hsl(var(--muted-foreground))]">Employees</div><div className="mt-1 font-medium">48 planned</div></div></div></section>;
}

function UpcomingCard() {
  return <section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><h2 className="font-display text-base font-bold">Next on the calendar</h2><Link href="/inspections" data-testid="link-all-calendar" className="text-xs font-semibold text-[hsl(var(--primary))]">See calendar</Link></div><div className="mt-4 flex gap-3 rounded-xl bg-[hsl(var(--muted))] p-3"><div className="grid h-11 w-11 place-items-center rounded-lg bg-[hsl(var(--primary))] text-center text-white"><span className="block text-[9px] uppercase opacity-70">Jun</span><span className="cg-number text-lg font-bold leading-none">20</span></div><div><div className="text-sm font-semibold">Joint site inspection</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">10:30 AM · GreenTech facility</div><Badge tone="navy">In 11 days</Badge></div></div></section>;
}

function IncentiveCallout() {
  return <section className="overflow-hidden rounded-xl bg-[hsl(var(--primary))] p-5 text-[hsl(var(--primary-foreground))]"><div className="flex items-start justify-between"><div><div className="cg-kicker text-[hsl(var(--primary-foreground)/.6)]">Matched for you</div><h2 className="mt-2 font-display text-lg font-bold">₹42.5L in available incentives</h2><p className="mt-1 text-xs text-[hsl(var(--primary-foreground)/.7)]">Based on your sector, location and investment.</p></div><Sparkles className="text-[hsl(var(--accent))]" size={22} /></div><Link href="/incentives" data-testid="link-incentives-callout" className="mt-6 inline-flex items-center gap-2 text-xs font-bold text-[hsl(var(--accent))]">Explore matched schemes <ArrowRight size={14} /></Link></section>;
}

function Approvals() {
  const [filter, setFilter] = useState('All');
  const groups = ['All', 'Environment', 'Safety', 'Construction', 'Utilities'];
  const filtered = approvalData.filter(a => filter === 'All' || a.domain === filter);
  return <><PageHeader eyebrow="Approval command centre" title="Your approval roadmap" description="12 approvals make up your path to operations. We keep every dependency visible." actions={<Button onClick={() => document.getElementById('approval-list')?.scrollIntoView({ behavior: 'smooth' })} testId="button-jump-roadmap"><ListFilter size={15} />Jump to roadmap</Button>} /><div className="mb-6 flex gap-2 overflow-x-auto pb-1">{groups.map(item => <button key={item} onClick={() => setFilter(item)} data-testid={`filter-approval-${item.toLowerCase()}`} className={cn('whitespace-nowrap rounded-full px-3.5 py-2 text-xs font-semibold', filter === item ? 'bg-[hsl(var(--primary))] text-white' : 'border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-[hsl(var(--muted-foreground))]')}>{item}{item === 'All' && <span className="ml-1.5 opacity-60">12</span>}</button>)}</div><div className="mb-6 grid gap-3 sm:grid-cols-3"><StatCard label="Approved" value="4" helper="Ready to operate" icon={CheckCircle2} tone="teal" /><StatCard label="Under review" value="5" helper="With departments" icon={Clock3} tone="navy" /><StatCard label="Needs action" value="2" helper="From you" icon={AlertTriangle} tone="amber" /></div><section id="approval-list" className="overflow-hidden rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="border-b border-[hsl(var(--border))] px-5 py-4"><div className="cg-kicker">Roadmap · {filtered.length} shown</div></div><div className="divide-y divide-[hsl(var(--border))]">{filtered.map((approval, i) => <ApprovalRow key={approval.id} approval={approval} index={i} />)}</div></section></>;
}

function ApprovalRow({ approval, index }: { approval: typeof approvalData[number]; index: number }) {
  const Icon = approval.status === 'Approved' ? CheckCircle2 : approval.status === 'Action needed' ? AlertTriangle : Clock3;
  const tone = approval.status === 'Approved' ? 'teal' : approval.status === 'Action needed' ? 'amber' : 'navy';
  return <Link href={`/approvals/${approval.id}`} data-testid={`row-approval-${approval.id}`} className="group grid gap-4 px-5 py-5 transition-colors hover:bg-[hsl(var(--muted)/.55)] md:grid-cols-[36px_1fr_180px_110px] md:items-center"><span className={cn('grid h-9 w-9 place-items-center rounded-lg', tone === 'teal' ? 'bg-[hsl(var(--chart-3)/.13)] text-[hsl(var(--chart-3))]' : tone === 'amber' ? 'bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]' : 'bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]')}><Icon size={17} /></span><span><span className="block text-sm font-semibold">{approval.name}</span><span className="mt-1 block text-xs text-[hsl(var(--muted-foreground))]">{approval.agency} <span className="mx-1 opacity-40">·</span> {approval.domain}</span><span className="mt-3 flex items-center gap-2"><span className="h-1.5 max-w-[210px] flex-1 rounded-full bg-[hsl(var(--muted))]"><span className={cn('block h-full rounded-full', tone === 'teal' ? 'bg-[hsl(var(--chart-3))]' : tone === 'amber' ? 'bg-[hsl(var(--accent))]' : 'bg-[hsl(var(--primary))]')} style={{ width: `${approval.progress}%` }} /></span><span className="cg-number text-[10px] text-[hsl(var(--muted-foreground))]">{approval.progress}%</span></span></span><span><Badge tone={tone}>{approval.status}</Badge><span className="mt-2 block text-xs text-[hsl(var(--muted-foreground))]">{approval.due}</span></span><span className="hidden justify-end text-[hsl(var(--muted-foreground))] group-hover:flex"><ArrowRight size={17} /></span></Link>;
}

function ApprovalDetail() {
  const params = useParams<{ id: string }>(); const approval = approvalData.find(a => a.id === params.id) ?? approvalData[0]; const [uploaded, setUploaded] = usePersistentState(`approval-${approval.id}-uploaded`, false); const [tab, setTab] = useState('Requirements');
  return <><Link href="/approvals" data-testid="link-back-approvals" className="mb-6 inline-flex items-center gap-2 text-xs font-semibold text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))]">← All approvals</Link><PageHeader eyebrow={`${approval.domain} · ${approval.initials}`} title={approval.name} description={approval.agency} actions={<><Button variant="outline" testId="button-contact-agency"><MessageSquareText size={15} />Contact agency</Button><Button testId="button-upload-approval" onClick={() => setUploaded(true)}><Upload size={15} />Upload document</Button></>} /><div className="mb-6 flex flex-wrap items-center gap-3"><Badge tone={approval.status === 'Approved' ? 'teal' : approval.status === 'Action needed' ? 'amber' : 'navy'}>{approval.status}</Badge><span className="text-xs text-[hsl(var(--muted-foreground))]">Application APP-24091</span><span className="text-xs text-[hsl(var(--muted-foreground))]">Last updated today at 08:42</span></div><div className="grid gap-5 xl:grid-cols-[1.35fr_.65fr]"><div className="space-y-5"><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex flex-wrap gap-1 border-b border-[hsl(var(--border))] pb-3">{['Requirements', 'Documents', 'Timeline'].map(item => <button key={item} onClick={() => setTab(item)} data-testid={`tab-approval-${item.toLowerCase()}`} className={cn('rounded-lg px-3 py-2 text-xs font-semibold', tab === item ? 'bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]' : 'text-[hsl(var(--muted-foreground))]')}>{item}</button>)}</div>{tab === 'Requirements' && <div className="mt-5 space-y-4"><Requirement done text="Application form and fee receipt" /><Requirement done text="Land ownership deed" /><Requirement done text="Factory layout — Rev 03" /><Requirement text="Revised environmental management plan" action={uploaded ? 'Uploaded just now' : 'Required before scrutiny'} /><Requirement text="Water source declaration" action="Optional supporting document" /></div>}{tab === 'Documents' && <div className="mt-4 space-y-2">{documentData.slice(0, 4).map(doc => <DocumentRow key={doc.id} doc={doc} />)}</div>}{tab === 'Timeline' && <Timeline />}</section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-start gap-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]"><Sparkles size={15} /></span><div><h3 className="font-semibold">Seva bot recommendation</h3><p className="mt-1 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">Upload the revised environmental plan next. It is the only blocking item for the department to complete its first scrutiny.</p></div></div></section></div><aside className="space-y-5"><section className="rounded-xl bg-[hsl(var(--primary))] p-5 text-white"><div className="cg-kicker text-white/55">Approval progress</div><div className="cg-number mt-4 text-4xl font-semibold">{approval.progress}%</div><div className="mt-3 h-1.5 rounded-full bg-white/15"><div className="h-full rounded-full bg-[hsl(var(--accent))]" style={{ width: `${approval.progress}%` }} /></div><div className="mt-4 flex justify-between text-xs text-white/65"><span>Started 02 Jun</span><span>Due 18 Jun</span></div></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h3 className="font-display font-bold">Agency contact</h3><div className="mt-4 text-sm font-semibold">Priya Nair</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Scrutiny officer · TNPCB Coimbatore</div><button onClick={() => window.alert('Message draft opened for Priya Nair.')} data-testid="button-message-officer" className="mt-4 flex items-center gap-2 text-xs font-bold text-[hsl(var(--primary))]"><MessageSquareText size={14} />Send a message</button></section></aside></div></>;
}

function Requirement({ done, text, action }: { done?: boolean; text: string; action?: string }) { return <div className="flex items-center gap-3"><span className={cn('grid h-7 w-7 shrink-0 place-items-center rounded-full border', done ? 'border-[hsl(var(--chart-3))] bg-[hsl(var(--chart-3))] text-white' : 'border-[hsl(var(--border))] text-transparent')}>{done && <Check size={14} />}</span><span className={cn('flex-1 text-sm', done && 'text-[hsl(var(--muted-foreground))] line-through')}>{text}</span>{!done && action && <span className="text-right text-[11px] text-[hsl(var(--destructive))]">{action}</span>}</div>; }
function Timeline() { return <div className="mt-5 space-y-5">{[{ title: 'Document requested', date: '09 Jun 2025 · 08:42 AM', active: true }, { title: 'Application under scrutiny', date: '06 Jun 2025', active: false }, { title: 'Application submitted', date: '02 Jun 2025', active: false }].map(item => <div className="flex gap-3" key={item.title}><span className={cn('mt-1 h-2.5 w-2.5 rounded-full', item.active ? 'bg-[hsl(var(--accent))]' : 'bg-[hsl(var(--border))]')} /><div><div className="text-sm font-semibold">{item.title}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{item.date}</div></div></div>)}</div>; }

function Applications() {
  const [tab, setTab] = useState('All applications'); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('All statuses');
  const shown = applications.filter(a => (tab === 'All applications' || (tab === 'Needs action' && a.status === 'Action needed') || (tab === 'Approved' && a.status === 'Approved')) && `${a.name} ${a.agency}`.toLowerCase().includes(query.toLowerCase()) && (filter === 'All statuses' || a.status === filter));
  return <><PageHeader eyebrow="Application tracker" title="Applications" description="One clear view of every submission, from first draft to approval." actions={<Button onClick={() => window.alert('New application flow opened.')} testId="button-new-application"><Plus size={15} />New application</Button>} /><div className="mb-5 flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="flex gap-1 overflow-x-auto">{['All applications', 'Needs action', 'Approved'].map(item => <button key={item} onClick={() => setTab(item)} data-testid={`tab-applications-${item.toLowerCase().replaceAll(' ', '-')}`} className={cn('whitespace-nowrap border-b-2 px-3 py-2.5 text-xs font-semibold', tab === item ? 'border-[hsl(var(--accent))] text-[hsl(var(--foreground))]' : 'border-transparent text-[hsl(var(--muted-foreground))]')}>{item}</button>)}</div><div className="flex gap-2"><div className="flex min-w-0 items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3"><Search size={15} className="text-[hsl(var(--muted-foreground))]" /><input value={query} onChange={e => setQuery(e.target.value)} data-testid="input-application-search" placeholder="Search applications" className="h-9 min-w-0 bg-transparent text-xs outline-none" /></div><select value={filter} onChange={e => setFilter(e.target.value)} data-testid="select-application-status" className="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 text-xs"><option>All statuses</option><option>Approved</option><option>Under review</option><option>Action needed</option><option>Documents requested</option></select></div></div><section className="overflow-x-auto rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-[hsl(var(--border))] bg-[hsl(var(--muted)/.5)] text-xs text-[hsl(var(--muted-foreground))]"><tr><th className="px-5 py-3 font-semibold">Application</th><th className="px-4 py-3 font-semibold">Status</th><th className="px-4 py-3 font-semibold">Submitted</th><th className="px-4 py-3 font-semibold">Last update</th><th className="px-4 py-3 font-semibold">Risk</th><th /></tr></thead><tbody className="divide-y divide-[hsl(var(--border))]">{shown.map(app => <tr key={app.id} className="hover:bg-[hsl(var(--muted)/.45)]" data-testid={`row-application-${app.id}`}><td className="px-5 py-4"><Link href="/approvals/pollution-consent" className="font-semibold hover:underline">{app.name}</Link><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{app.id} · {app.agency}</div></td><td className="px-4 py-4"><Badge tone={app.status === 'Approved' ? 'teal' : app.status === 'Action needed' ? 'amber' : app.status === 'Documents requested' ? 'red' : 'navy'}>{app.status}</Badge></td><td className="px-4 py-4 text-xs text-[hsl(var(--muted-foreground))]">{app.submitted}</td><td className="px-4 py-4 text-xs text-[hsl(var(--muted-foreground))]">{app.updated}</td><td className="px-4 py-4"><Badge tone={app.risk === 'High' ? 'red' : app.risk === 'Medium' ? 'amber' : 'slate'}>{app.risk}</Badge></td><td className="px-4 py-4 text-right"><Link href="/approvals/pollution-consent" data-testid={`link-application-${app.id}`}><ChevronRight size={16} className="text-[hsl(var(--muted-foreground))]" /></Link></td></tr>)}</tbody></table>{!shown.length && <EmptyState title="No applications match" description="Try changing the tab or status filter." />}</section></>;
}

function DocumentRow({ doc }: { doc: typeof documentData[number] }) { const Icon = doc.icon; return <div className="flex items-center gap-3 rounded-lg p-2 hover:bg-[hsl(var(--muted))]"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><Icon size={16} /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold">{doc.name}</span><span className="block text-xs text-[hsl(var(--muted-foreground))]">{doc.type} · {doc.size}</span></span><Badge tone={doc.status === 'Verified' ? 'teal' : doc.status === 'Needs update' ? 'amber' : 'red'}>{doc.status}</Badge><button onClick={() => window.alert(`${doc.name} preview opened.`)} data-testid={`button-preview-${doc.id}`} className="rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><ExternalLink size={15} /></button></div>; }

function Documents() {
  const [type, setType] = useState('All types'); const [uploading, setUploading] = useState(false); const [query, setQuery] = useState('');
  const shown = documentData.filter(d => (type === 'All types' || d.type === type) && d.name.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeader eyebrow="Central document vault" title="Documents" description="One trusted source for every file you submit, reuse and share." actions={<Button onClick={() => setUploading(true)} testId="button-upload-vault"><CloudUpload size={15} />Upload documents</Button>} />{uploading && <div className="mb-5 flex items-center gap-3 rounded-xl border border-[hsl(var(--accent)/.6)] bg-[hsl(var(--accent)/.1)] p-4"><CloudUpload size={18} className="text-[hsl(32_70%_32%)]" /><div className="flex-1 text-sm"><b>Drop files here</b> or choose files from your device. Accepted: PDF, JPG, PNG up to 10 MB.</div><input type="file" multiple data-testid="input-file-upload" /><button onClick={() => setUploading(false)} data-testid="button-close-upload"><X size={16} /></button></div>}<div className="mb-5 grid gap-3 sm:grid-cols-3"><StatCard label="Total documents" value="34" helper="Across 7 applications" icon={FolderOpen} /><StatCard label="Verified" value="29" helper="Ready to reuse" icon={FileCheck2} tone="teal" /><StatCard label="Need attention" value="3" helper="Update before submission" icon={AlertTriangle} tone="amber" /></div><div className="mb-4 flex flex-col gap-3 sm:flex-row"><div className="flex flex-1 items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3"><Search size={15} className="text-[hsl(var(--muted-foreground))]" /><input value={query} onChange={e => setQuery(e.target.value)} data-testid="input-document-search" placeholder="Search your vault" className="h-10 flex-1 bg-transparent text-sm outline-none" /></div><select value={type} onChange={e => setType(e.target.value)} data-testid="select-document-type" className="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 text-sm"><option>All types</option><option>Legal</option><option>Technical</option><option>Environment</option><option>Safety</option><option>Company</option><option>Utilities</option></select></div><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3">{shown.map(doc => <DocumentRow key={doc.id} doc={doc} />)}</section></>;
}

function Inspections() {
  const [, setLocation] = useLocation(); return <><PageHeader eyebrow="Site readiness" title="Inspections" description="Prepare once, coordinate every department, and keep the visit predictable." actions={<Button onClick={() => window.alert('Inspection request form opened.')} testId="button-request-inspection"><Plus size={15} />Request inspection</Button>} /><div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-start justify-between"><div><div className="cg-kicker mb-2">Upcoming · 20 June 2025</div><h2 className="font-display text-xl font-bold">Joint site inspection</h2><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">TNPCB · DISH · TNFRS</p></div><Badge tone="amber">In 11 days</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-[hsl(var(--muted))] p-3"><CalendarDays size={16} /><div className="mt-3 text-xs text-[hsl(var(--muted-foreground))]">Date & time</div><div className="mt-1 text-sm font-semibold">20 Jun · 10:30 AM</div></div><div className="rounded-lg bg-[hsl(var(--muted))] p-3"><Building2 size={16} /><div className="mt-3 text-xs text-[hsl(var(--muted-foreground))]">Location</div><div className="mt-1 text-sm font-semibold">GreenTech facility</div></div><div className="rounded-lg bg-[hsl(var(--muted))] p-3"><UsersRound size={16} /><div className="mt-3 text-xs text-[hsl(var(--muted-foreground))]">Officers</div><div className="mt-1 text-sm font-semibold">3 attending</div></div></div><div className="mt-6 border-t border-[hsl(var(--border))] pt-5"><div className="flex items-center justify-between"><h3 className="font-semibold">Readiness checklist</h3><span className="cg-number text-xs text-[hsl(var(--muted-foreground))]">4 / 6</span></div><div className="mt-3 h-2 rounded-full bg-[hsl(var(--muted))]"><div className="h-full w-2/3 rounded-full bg-[hsl(var(--chart-3))]" /></div><div className="mt-4 space-y-3"><Requirement done text="Access route and safety signage" /><Requirement done text="Fire equipment certificate available" /><Requirement done text="Factory layout — Rev 03" /><Requirement done text="Responsible person confirmed" /><Requirement text="Environmental plan on site" /><Requirement text="Production area housekeeping" /></div><Button onClick={() => setLocation('/documents')} variant="outline" className="mt-5" testId="button-inspection-documents">View required documents <ArrowRight size={14} /></Button></div></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-start gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]"><Sparkles size={17} /></span><div><div className="cg-kicker">Combined recommendation</div><h2 className="mt-1 font-display text-lg font-bold">One visit can clear 3 checks</h2><p className="mt-2 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">Your TNPCB, DISH and Fire Safety requirements overlap. Keep these five documents ready to reduce repeat visits.</p></div></div><div className="mt-6 space-y-3">{['Factory layout — Rev 03', 'Environmental management plan', 'Fire equipment certificate', 'Land ownership deed', 'Water source declaration'].map((x, i) => <div key={x} className="flex items-center gap-3 text-sm"><span className="cg-number grid h-6 w-6 place-items-center rounded-full bg-[hsl(var(--muted))] text-[10px]">{i + 1}</span>{x}<CheckCircle2 size={15} className="ml-auto text-[hsl(var(--chart-3))]" /></div>)}</div><Button onClick={() => window.alert('Readiness pack downloaded.')} variant="outline" className="mt-6 w-full" testId="button-download-readiness"><Download size={15} />Download readiness pack</Button></section></div></>;
}

function Renewals() {
  const [reminded, setReminded] = usePersistentState<string[]>('renewal-reminders', []); const renewals = [{ name: 'Water source declaration', agency: 'TNPCB', date: '28 Jun 2025', days: '19 days', tone: 'red' as Tone }, { name: 'Factory licence', agency: 'DISH', date: '14 Aug 2025', days: '66 days', tone: 'amber' as Tone }, { name: 'Fire equipment certificate', agency: 'TNFRS', date: '28 Apr 2026', days: '323 days', tone: 'teal' as Tone }];
  return <><PageHeader eyebrow="Expiry management" title="Renewals" description="Stay ahead of certificates and recurring permissions before they become blockers." actions={<Button variant="outline" onClick={() => window.alert('Renewal calendar exported.')} testId="button-export-renewals"><Download size={15} />Export calendar</Button>} /><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] p-5"><div><h2 className="font-display font-bold">Renewal queue</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Sorted by urgency</p></div><Badge tone="red">1 urgent</Badge></div><div className="divide-y divide-[hsl(var(--border))]">{renewals.map(item => <div className="flex flex-wrap items-center gap-4 p-5" key={item.name}><span className={cn('grid h-10 w-10 place-items-center rounded-lg', item.tone === 'red' ? 'bg-[hsl(var(--destructive)/.1)] text-[hsl(var(--destructive))]' : item.tone === 'amber' ? 'bg-[hsl(var(--accent)/.18)] text-[hsl(32_70%_32%)]' : 'bg-[hsl(var(--chart-3)/.13)] text-[hsl(var(--chart-3))]')}><RefreshCw size={17} /></span><div className="min-w-[200px] flex-1"><div className="text-sm font-semibold">{item.name}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{item.agency} · expires {item.date}</div></div><Badge tone={item.tone}>{item.days}</Badge><Button variant={reminded.includes(item.name) ? 'outline' : 'ghost'} onClick={() => setReminded([...reminded, item.name])} testId={`button-remind-${item.name.toLowerCase().replaceAll(' ', '-')}`}>{reminded.includes(item.name) ? <><Check size={14} />Reminder set</> : <><Bell size={14} />Remind me</>}</Button></div>)}</div></section></>;
}

function Incentives() {
  const schemes = [{ title: 'Tamil Nadu Investment Promotion Subsidy', body: 'Capital subsidy for eligible manufacturing investments in notified districts.', amount: 'Up to ₹30L', match: '94% match', tags: ['Manufacturing', 'Coimbatore'], tone: 'teal' as Tone }, { title: 'Food Processing Infrastructure Support', body: 'Support for plant and machinery in value-added food processing units.', amount: 'Up to ₹12.5L', match: '87% match', tags: ['Food processing', 'MSME'], tone: 'amber' as Tone }, { title: 'Green Industry Power Rebate', body: 'Energy efficiency incentive for units investing in cleaner production systems.', amount: 'Up to ₹8L', match: '72% match', tags: ['Energy', 'Sustainability'], tone: 'navy' as Tone }];
  return <><PageHeader eyebrow="Scheme discovery" title="Incentives" description="Government support matched to GreenTech's sector, location and investment profile." actions={<Button variant="outline" onClick={() => window.alert('Eligibility profile editor opened.')} testId="button-edit-eligibility"><Settings size={15} />Edit eligibility profile</Button>} /><div className="mb-6 rounded-xl bg-[hsl(var(--primary))] p-5 text-white md:flex md:items-center md:justify-between"><div><div className="cg-kicker text-white/55">Your matched opportunity</div><div className="mt-2 font-display text-2xl font-bold">₹42.5L potential support</div><p className="mt-1 text-sm text-white/65">Across 3 schemes with a strong eligibility fit.</p></div><Target size={45} className="mt-4 text-[hsl(var(--accent))] md:mt-0" /></div><div className="grid gap-4 lg:grid-cols-3">{schemes.map(scheme => <section key={scheme.title} className="flex flex-col rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 hover:-translate-y-0.5"><div className="flex items-center justify-between"><Badge tone={scheme.tone}>{scheme.match}</Badge><MoreHorizontal size={17} className="text-[hsl(var(--muted-foreground))]" /></div><h2 className="mt-5 font-display text-lg font-bold">{scheme.title}</h2><p className="mt-2 flex-1 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">{scheme.body}</p><div className="mt-5 text-2xl font-semibold text-[hsl(var(--primary))]">{scheme.amount}</div><div className="mt-4 flex flex-wrap gap-1.5">{scheme.tags.map(tag => <span key={tag} className="rounded-full bg-[hsl(var(--muted))] px-2.5 py-1 text-[10px] font-medium">{tag}</span>)}</div><Button onClick={() => window.alert(`Eligibility check started for ${scheme.title}.`)} variant="outline" className="mt-5 w-full" testId={`button-check-${scheme.title.toLowerCase().replaceAll(' ', '-')}`}>Check eligibility <ArrowRight size={14} /></Button></section>)}</div></>;
}

function Compliance() {
  const alerts = [{ name: 'Environmental plan update', detail: 'Required by TNPCB before scrutiny', tone: 'amber' as Tone }, { name: 'Water declaration expiry', detail: 'Expires in 19 days', tone: 'red' as Tone }];
  return <><PageHeader eyebrow="Operating confidence" title="Compliance health" description="A simple pulse check across active obligations and submitted evidence." actions={<Button variant="outline" onClick={() => window.alert('Compliance report downloaded.')} testId="button-download-compliance"><Download size={15} />Download report</Button>} /><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><section className="relative overflow-hidden rounded-xl bg-[hsl(var(--primary))] p-6 text-white"><div className="cg-kicker text-white/55">Overall health score</div><div className="mt-5 flex items-end gap-2"><span className="cg-number text-6xl font-semibold">86</span><span className="mb-2 text-lg text-white/60">/ 100</span></div><div className="mt-2 text-sm text-white/65">Good standing · last checked today</div><div className="mt-8 grid grid-cols-3 gap-2 text-center text-xs"><div className="rounded-lg bg-white/10 p-3"><div className="cg-number text-xl font-bold">29</div><div className="mt-1 text-white/60">Verified</div></div><div className="rounded-lg bg-white/10 p-3"><div className="cg-number text-xl font-bold">3</div><div className="mt-1 text-white/60">Due soon</div></div><div className="rounded-lg bg-white/10 p-3"><div className="cg-number text-xl font-bold">2</div><div className="mt-1 text-white/60">Alerts</div></div></div></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><h2 className="font-display font-bold">Needs attention</h2><Badge tone="amber">2 open alerts</Badge></div><div className="mt-4 divide-y divide-[hsl(var(--border))]">{alerts.map((alert, i) => <div className="flex items-center gap-3 py-4" key={alert.name}><span className={cn('grid h-9 w-9 place-items-center rounded-lg', alert.tone === 'red' ? 'bg-[hsl(var(--destructive)/.1)] text-[hsl(var(--destructive))]' : 'bg-[hsl(var(--accent)/.18)] text-[hsl(32_70%_32%)]')}><AlertTriangle size={16} /></span><div className="flex-1"><div className="text-sm font-semibold">{alert.name}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{alert.detail}</div></div><Link href={i ? '/renewals' : '/approvals/pollution-consent'} data-testid={`link-compliance-alert-${i}`}><ArrowRight size={16} /></Link></div>)}</div></section></div><section className="mt-5 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><div><h2 className="font-display font-bold">Control areas</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Evidence coverage by operating area</p></div><span className="text-xs text-[hsl(var(--muted-foreground))]">Updated 09 Jun</span></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Environment', 72, 'amber'], ['Safety', 100, 'teal'], ['Legal & land', 96, 'teal'], ['Utilities', 82, 'navy']].map(([name, value, tone]) => <div key={name as string}><div className="flex justify-between text-sm font-medium"><span>{name}</span><span className="cg-number text-xs">{value}%</span></div><div className="mt-2 h-2 rounded-full bg-[hsl(var(--muted))]"><div className={cn('h-full rounded-full', tone === 'teal' ? 'bg-[hsl(var(--chart-3))]' : tone === 'amber' ? 'bg-[hsl(var(--accent))]' : 'bg-[hsl(var(--primary))]')} style={{ width: `${value}%` }} /></div></div>)}</div></section></>;
}

function Grievances() {
  const [open, setOpen] = useState(false); const [raised, setRaised] = usePersistentState('grievance-raised', false);
  return <><PageHeader eyebrow="Resolution desk" title="Grievances" description="Track questions and escalations with a clear owner and next step." actions={<Button onClick={() => setOpen(true)} testId="button-raise-grievance"><Plus size={15} />Raise a grievance</Button>} /><section className="grid gap-4 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] p-5"><h2 className="font-display font-bold">Your cases</h2><Badge tone="navy">2 active</Badge></div>{(raised ? [{ id: 'GRV-1083', title: 'Follow-up on approval review', agency: 'TNPCB', status: 'Raised today' }, ...[]] : [{ id: 'GRV-1077', title: 'Clarification on inspection scope', agency: 'DISH', status: 'In progress' }, { id: 'GRV-1062', title: 'Application fee receipt mismatch', agency: 'CCMC', status: 'Resolved' }]).map(item => <div className="flex items-center gap-3 border-b border-[hsl(var(--border))] p-5 last:border-0" key={item.id}><span className="grid h-9 w-9 place-items-center rounded-lg bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><MessageSquareText size={16} /></span><div className="flex-1"><div className="text-sm font-semibold">{item.title}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{item.id} · {item.agency}</div></div><Badge tone={item.status === 'Resolved' ? 'teal' : 'amber'}>{item.status}</Badge><ChevronRight size={16} /></div>)}</div><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="cg-kicker mb-2">Selected case · GRV-1077</div><h2 className="font-display text-lg font-bold">Clarification on inspection scope</h2><p className="mt-2 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">We need clarity on whether the storage annex is included in the upcoming joint visit.</p><div className="mt-6 space-y-5 border-l border-[hsl(var(--border))] pl-5"><div className="relative"><span className="absolute -left-[25px] top-1 h-2.5 w-2.5 rounded-full bg-[hsl(var(--chart-3))]" /><div className="text-sm font-semibold">Officer response received</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">06 Jun · DISH confirmed annex scope</div></div><div className="relative"><span className="absolute -left-[25px] top-1 h-2.5 w-2.5 rounded-full bg-[hsl(var(--border))]" /><div className="text-sm font-semibold">Case raised</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">04 Jun · Quantum Thinkers</div></div></div><Button variant="outline" onClick={() => window.alert('Reply composer opened.')} className="mt-6" testId="button-reply-grievance"><MessageSquareText size={14} />Reply to case</Button></section></section>{open && <Modal title="Raise a grievance" onClose={() => setOpen(false)}><label className="block text-xs font-semibold">Subject<input data-testid="input-grievance-subject" className="mt-2 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent p-2.5 text-sm outline-none" placeholder="What do you need help with?" /></label><label className="mt-4 block text-xs font-semibold">Details<textarea data-testid="input-grievance-details" className="mt-2 min-h-24 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent p-2.5 text-sm outline-none" placeholder="Add context so the department can respond faster." /></label><Button onClick={() => { setRaised(true); setOpen(false); }} className="mt-5 w-full" testId="button-submit-grievance">Submit grievance</Button></Modal>}</>;
}

function Notifications() {
  const [items, setItems] = usePersistentState('notifications', notifyData); return <><PageHeader eyebrow="Signal centre" title="Notifications" description="Priority updates from your applications, departments and deadlines." actions={<Button variant="outline" onClick={() => setItems(items.map(item => ({ ...item, unread: false })))} testId="button-mark-read"><Check size={15} />Mark all as read</Button>} /><div className="mb-5 flex gap-2"><Badge tone="amber">{items.filter(item => item.unread).length} unread</Badge><span className="text-xs text-[hsl(var(--muted-foreground))]">Sorted by priority</span></div><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]">{items.map(item => <Link href={item.link} data-testid={`notification-${item.id}`} key={item.id} className="flex gap-4 border-b border-[hsl(var(--border))] p-5 last:border-0 hover:bg-[hsl(var(--muted)/.45)]"><span className={cn('mt-1 grid h-9 w-9 shrink-0 place-items-center rounded-lg', item.tone === 'red' ? 'bg-[hsl(var(--destructive)/.1)] text-[hsl(var(--destructive))]' : item.tone === 'amber' ? 'bg-[hsl(var(--accent)/.2)] text-[hsl(32_70%_32%)]' : item.tone === 'teal' ? 'bg-[hsl(var(--chart-3)/.13)] text-[hsl(var(--chart-3))]' : 'bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]')}><Bell size={16} /></span><span className="flex-1"><span className="flex items-center gap-2 text-sm font-semibold">{item.title}{item.unread && <span className="h-1.5 w-1.5 rounded-full bg-[hsl(var(--accent))]" />}</span><span className="mt-1 block text-sm text-[hsl(var(--muted-foreground))]">{item.body}</span><span className="mt-2 block text-xs text-[hsl(var(--muted-foreground))]">{item.time}</span></span><ChevronRight size={16} className="mt-1 text-[hsl(var(--muted-foreground))]" /></Link>)}</section></>;
}

function Analytics() {
  const trend = [{ month: 'Jan', approvals: 14, completed: 8 }, { month: 'Feb', approvals: 19, completed: 12 }, { month: 'Mar', approvals: 24, completed: 17 }, { month: 'Apr', approvals: 21, completed: 19 }, { month: 'May', approvals: 29, completed: 23 }, { month: 'Jun', approvals: 31, completed: 27 }]; const split = [{ name: 'Approved', value: 42, color: 'hsl(var(--chart-3))' }, { name: 'Under review', value: 31, color: 'hsl(var(--primary))' }, { name: 'Action needed', value: 17, color: 'hsl(var(--accent))' }, { name: 'Returned', value: 10, color: 'hsl(var(--destructive))' }];
  return <><PageHeader eyebrow="Operations intelligence" title="Analytics" description="A department-wide view of throughput, risk and turnaround." actions={<div className="flex items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 py-2 text-xs"><CalendarDays size={14} />Last 6 months<ChevronDown size={13} /></div>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Applications processed" value="138" helper="+18.4% vs previous period" icon={ClipboardCheck} tone="teal" /><StatCard label="Median turnaround" value="12.4d" helper="2.1 days faster" icon={Clock3} tone="navy" /><StatCard label="At-risk queue" value="17" helper="Needs intervention" icon={AlertTriangle} tone="amber" /><StatCard label="SLA adherence" value="91.8%" helper="Target 90%" icon={Gauge} tone="teal" /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.4fr_.6fr]"><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-display font-bold">Application throughput</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Volume submitted vs completed</p></div><div className="flex gap-4 text-xs"><span><i className="mr-1.5 inline-block h-2 w-2 rounded-full bg-[hsl(var(--primary))]" />Submitted</span><span><i className="mr-1.5 inline-block h-2 w-2 rounded-full bg-[hsl(var(--chart-3))]" />Completed</span></div></div><div className="h-[280px]"><ResponsiveContainer width="100%" height="100%"><AreaChart data={trend}><CartesianGrid stroke="hsl(var(--border))" strokeDasharray="3 3" vertical={false} /><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 11 }} /><YAxis axisLine={false} tickLine={false} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 11 }} /><ChartTooltip contentStyle={{ background: 'hsl(var(--card))', border: '1px solid hsl(var(--border))', borderRadius: 8, fontSize: 12 }} /><Area type="monotone" dataKey="approvals" stroke="hsl(var(--primary))" fill="hsl(var(--primary) / .08)" strokeWidth={2} /><Area type="monotone" dataKey="completed" stroke="hsl(var(--chart-3))" fill="hsl(var(--chart-3) / .08)" strokeWidth={2} /></AreaChart></ResponsiveContainer></div></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h2 className="font-display font-bold">Queue composition</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Current application status</p><div className="relative mt-3 h-[220px]"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={split} dataKey="value" nameKey="name" cx="50%" cy="50%" innerRadius={60} outerRadius={86} paddingAngle={3}>{split.map(item => <Cell key={item.name} fill={item.color} />)}</Pie><ChartTooltip /></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 grid place-items-center"><div className="text-center"><div className="cg-number text-2xl font-semibold">100</div><div className="text-[10px] text-[hsl(var(--muted-foreground))]">total</div></div></div></div><div className="space-y-2">{split.map(item => <div key={item.name} className="flex items-center justify-between text-xs"><span><i className="mr-2 inline-block h-2 w-2 rounded-full" style={{ backgroundColor: item.color }} />{item.name}</span><span className="cg-number font-semibold">{item.value}%</span></div>)}</div></section></div><section className="mt-5 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h2 className="font-display font-bold">Department turnaround</h2><div className="mt-5 h-[240px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={[{ name: 'TNPCB', days: 16 }, { name: 'DISH', days: 11 }, { name: 'CCMC', days: 8 }, { name: 'TNFRS', days: 6 }, { name: 'TANGEDCO', days: 14 }]} layout="vertical"><CartesianGrid stroke="hsl(var(--border))" horizontal={false} /><XAxis type="number" axisLine={false} tickLine={false} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 11 }} /><YAxis dataKey="name" type="category" axisLine={false} tickLine={false} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 11 }} width={72} /><ChartTooltip /><Bar dataKey="days" fill="hsl(var(--primary))" radius={[0, 5, 5, 0]} barSize={22} /></BarChart></ResponsiveContainer></div></section></>;
}

function Projects() {
  const [, setLocation] = useLocation(); return <><PageHeader eyebrow="Project portfolio" title="Projects" description="Keep project scope, location and approvals connected." actions={<Button onClick={() => window.alert('New project form opened.')} testId="button-new-project"><Plus size={15} />New project</Button>} /><section className="grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6"><div className="flex items-start justify-between"><div><Badge tone="teal">Active project</Badge><h2 className="mt-4 font-display text-2xl font-bold">GreenTech Manufacturing Unit</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">A modern food processing facility in Coimbatore, Tamil Nadu.</p></div><button onClick={() => setLocation('/settings')} data-testid="button-project-more"><MoreHorizontal size={18} /></button></div><div className="mt-8 grid grid-cols-2 gap-y-5 text-sm sm:grid-cols-4"><div><div className="cg-kicker">Investment</div><div className="mt-2 font-semibold">₹8.5 Crore</div></div><div><div className="cg-kicker">Progress</div><div className="mt-2 font-semibold">68%</div></div><div><div className="cg-kicker">Start date</div><div className="mt-2 font-semibold">Jan 2025</div></div><div><div className="cg-kicker">Target</div><div className="mt-2 font-semibold">Dec 2025</div></div></div><div className="mt-8 h-2 rounded-full bg-[hsl(var(--muted))]"><div className="h-full w-[68%] rounded-full bg-[hsl(var(--chart-3))]" /></div><Button onClick={() => setLocation('/approvals')} variant="outline" className="mt-7" testId="button-project-roadmap">View approval roadmap <ArrowRight size={14} /></Button></div><div className="rounded-xl bg-[hsl(var(--primary))] p-6 text-white"><div className="cg-kicker text-white/55">Project facts</div><div className="mt-5 space-y-4">{[['Location', 'Coimbatore · Tamil Nadu'], ['Industry', 'Manufacturing / food processing'], ['Entity', 'GreenTech Manufacturing Pvt. Ltd.'], ['Planned jobs', '48 direct employees']].map(([key, value]) => <div className="border-b border-white/10 pb-3" key={key}><div className="text-xs text-white/55">{key}</div><div className="mt-1 text-sm font-semibold">{value}</div></div>)}</div></div></section><div className="mt-5 grid gap-4 md:grid-cols-3"><Link href="/approvals" data-testid="card-project-approvals" className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 hover:-translate-y-0.5"><Gavel size={19} /><h3 className="mt-4 font-semibold">12 approvals</h3><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">4 complete · 2 need action</p></Link><Link href="/documents" data-testid="card-project-documents" className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 hover:-translate-y-0.5"><FolderOpen size={19} /><h3 className="mt-4 font-semibold">34 documents</h3><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">29 verified and reusable</p></Link><Link href="/compliance" data-testid="card-project-compliance" className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 hover:-translate-y-0.5"><ShieldCheck size={19} /><h3 className="mt-4 font-semibold">86 / 100 health</h3><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Good standing</p></Link></div></>;
}

function Department() {
  const [, setLocation] = useLocation(); const rows = applications.map((a, i) => ({ ...a, risk: i === 3 ? 'High' : i === 0 ? 'Medium' : 'Low' })); return <><PageHeader eyebrow="Department operations" title="Good morning, Priya." description="DISH Coimbatore · Risk-based scrutiny queue" actions={<Button variant="outline" onClick={() => setLocation('/analytics')} testId="button-department-analytics"><BarChart3 size={15} />View analytics</Button>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Open queue" value="24" helper="6 received today" icon={ClipboardCheck} tone="navy" /><StatCard label="High risk" value="4" helper="Require senior review" icon={AlertTriangle} tone="red" /><StatCard label="Due this week" value="9" helper="SLA at risk for 2" icon={Clock3} tone="amber" /><StatCard label="SLA adherence" value="91.8%" helper="+4.2% this month" icon={Gauge} tone="teal" /></div><section className="mt-5 overflow-x-auto rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><div className="flex items-center justify-between border-b border-[hsl(var(--border))] p-5"><div><h2 className="font-display font-bold">Risk-based work queue</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Prioritised by deadline, completeness and project impact</p></div><div className="flex gap-2"><Button variant="outline" testId="button-queue-filter"><Filter size={14} />Filters</Button><Button onClick={() => window.alert('Bulk assignment opened.')} testId="button-bulk-assign"><UsersRound size={14} />Assign</Button></div></div><table className="w-full min-w-[780px] text-left text-sm"><thead className="border-b border-[hsl(var(--border))] bg-[hsl(var(--muted)/.5)] text-xs text-[hsl(var(--muted-foreground))]"><tr><th className="px-5 py-3">Application</th><th className="px-4 py-3">Applicant</th><th className="px-4 py-3">Risk</th><th className="px-4 py-3">Completeness</th><th className="px-4 py-3">SLA due</th><th /></tr></thead><tbody className="divide-y divide-[hsl(var(--border))]">{rows.map((row, i) => <tr key={row.id} className="hover:bg-[hsl(var(--muted)/.45)]" data-testid={`row-officer-${row.id}`}><td className="px-5 py-4"><Link href={`/department/applications/${row.id}`} className="font-semibold hover:underline">{row.name}</Link><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{row.id}</div></td><td className="px-4 py-4 text-xs">GreenTech Manufacturing</td><td className="px-4 py-4"><Badge tone={row.risk === 'High' ? 'red' : row.risk === 'Medium' ? 'amber' : 'teal'}>{row.risk}</Badge></td><td className="px-4 py-4"><div className="flex items-center gap-2"><span className="h-1.5 w-20 rounded-full bg-[hsl(var(--muted))]"><span className="block h-full rounded-full bg-[hsl(var(--chart-3))]" style={{ width: `${96 - i * 8}%` }} /></span><span className="cg-number text-xs">{96 - i * 8}%</span></div></td><td className="px-4 py-4 text-xs text-[hsl(var(--muted-foreground))]">{i === 3 ? 'Tomorrow' : `${3 + i} days`}</td><td className="px-4 py-4"><ChevronRight size={16} /></td></tr>)}</tbody></table></section></>;
}

function OfficerReview() {
  const [decision, setDecision] = useState(''); return <><Link href="/department" data-testid="link-back-department" className="mb-6 inline-flex items-center gap-2 text-xs font-semibold text-[hsl(var(--muted-foreground))]">← Back to queue</Link><PageHeader eyebrow="Officer review workspace" title="Consent to Establish" description="APP-24091 · GreenTech Manufacturing Pvt. Ltd." actions={<><Button variant="outline" onClick={() => setDecision('Request for information drafted')} testId="button-request-info"><MessageSquareText size={15} />Request information</Button><Button onClick={() => setDecision('Application marked recommended')} testId="button-recommend-approval"><CheckCircle2 size={15} />Recommend approval</Button></>} /><div className="grid gap-5 xl:grid-cols-[1fr_360px]"><div className="space-y-5"><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><h2 className="font-display font-bold">Application summary</h2><Badge tone="amber">Medium risk</Badge></div><div className="mt-5 grid gap-4 sm:grid-cols-3">{[['Applicant', 'Quantum Thinkers'], ['Project', 'GreenTech Manufacturing Unit'], ['Investment', '₹8.5 Crore'], ['Location', 'Coimbatore, Tamil Nadu'], ['Submitted', '02 Jun 2025'], ['SLA due', '18 Jun 2025']].map(([key, value]) => <div key={key}><div className="cg-kicker">{key}</div><div className="mt-1.5 text-sm font-semibold">{value}</div></div>)}</div></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><div className="flex items-center justify-between"><div><h2 className="font-display font-bold">Submitted evidence</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">4 of 5 required documents verified</p></div><Button variant="outline" onClick={() => window.alert('Document comparison opened.')} testId="button-compare-documents">Compare revisions</Button></div><div className="mt-4 space-y-2">{documentData.slice(0, 5).map(doc => <DocumentRow key={doc.id} doc={doc} />)}</div></section>{decision && <div role="status" data-testid="officer-decision" className="rounded-xl border border-[hsl(var(--chart-3)/.4)] bg-[hsl(var(--chart-3)/.1)] p-4 text-sm font-semibold text-[hsl(165_48%_29%)]"><CheckCircle2 className="mr-2 inline" size={16} />{decision}</div>}</div><aside className="space-y-5"><section className="rounded-xl bg-[hsl(var(--primary))] p-5 text-white"><div className="cg-kicker text-white/55">Scrutiny signal</div><div className="mt-3 text-2xl font-bold">Review recommended</div><p className="mt-2 text-sm leading-relaxed text-white/65">Evidence is strong. One environmental plan update remains before final recommendation.</p><Button onClick={() => setDecision('Application marked recommended')} className="mt-5 w-full bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]" testId="button-review-recommend">Recommend with condition</Button></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h2 className="font-display font-bold">Review trail</h2><Timeline /></section></aside></div></>;
}

function SettingsPage() {
  const [saved, setSaved] = useState(false); const [email, setEmail] = usePersistentState('settings-email-updates', true); const [sms, setSms] = usePersistentState('settings-sms-reminders', false); return <><PageHeader eyebrow="Workspace preferences" title="Settings" description="Manage your profile, notifications and account access." actions={saved ? <Badge tone="teal"><Check size={13} />Changes saved</Badge> : undefined} /><div className="grid gap-5 lg:grid-cols-[220px_1fr]"><nav className="space-y-1"><button className="w-full rounded-lg bg-[hsl(var(--secondary))] px-3 py-2.5 text-left text-sm font-semibold text-[hsl(var(--primary))]" data-testid="settings-tab-profile">Profile</button><button className="w-full rounded-lg px-3 py-2.5 text-left text-sm text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="settings-tab-notifications">Notifications</button><button className="w-full rounded-lg px-3 py-2.5 text-left text-sm text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="settings-tab-security">Security</button></nav><div className="space-y-5"><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h2 className="font-display font-bold">Profile details</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">This information appears on your applications.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold">Full name<input defaultValue="Quantum Thinkers" data-testid="input-settings-name" className="mt-2 h-10 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent px-3 text-sm outline-none" /></label><label className="text-xs font-semibold">Role<input defaultValue="Founder" data-testid="input-settings-role" className="mt-2 h-10 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent px-3 text-sm outline-none" /></label><label className="text-xs font-semibold">Email<input defaultValue="arun@greentech.in" data-testid="input-settings-email" className="mt-2 h-10 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent px-3 text-sm outline-none" /></label><label className="text-xs font-semibold">Phone<input defaultValue="+91 98430 21874" data-testid="input-settings-phone" className="mt-2 h-10 w-full rounded-lg border border-[hsl(var(--border))] bg-transparent px-3 text-sm outline-none" /></label></div><Button onClick={() => { setSaved(true); window.setTimeout(() => setSaved(false), 2500); }} className="mt-5" testId="button-save-settings">Save changes</Button></section><section className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5"><h2 className="font-display font-bold">Notification preferences</h2><div className="mt-4 divide-y divide-[hsl(var(--border))]">{[['Email updates', email, setEmail, 'Application and deadline updates'], ['SMS reminders', sms, setSms, 'Only urgent expiry reminders']].map(([label, value, setter, helper]) => <div className="flex items-center justify-between py-4" key={label as string}><div><div className="text-sm font-semibold">{label as string}</div><div className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{helper as string}</div></div><button onClick={() => (setter as (v: boolean) => void)(!(value as boolean))} data-testid={`toggle-${(label as string).toLowerCase().replace(' ', '-')}`} className={cn('relative h-6 w-11 rounded-full', value ? 'bg-[hsl(var(--primary))]' : 'bg-[hsl(var(--border))]')}><span className={cn('absolute top-1 h-4 w-4 rounded-full bg-white transition-transform', value ? 'translate-x-6' : 'translate-x-1')} /></button></div>)}</div></section></div></div></>;
}

function Login() {
  const [, setLocation] = useLocation(); const [mode, setMode] = useState<'login' | 'register'>('login'); const [path, setPath] = useState<'applicant' | 'officer'>('applicant');
  return <div className="grid min-h-[100dvh] lg:grid-cols-[.92fr_1.08fr]"><div className="relative hidden overflow-hidden bg-[hsl(var(--primary))] p-10 text-white lg:flex lg:flex-col"><div className="cg-grid absolute inset-0 opacity-10" /><div className="relative"><Logo dark /></div><div className="relative mt-auto max-w-lg pb-8"><div className="cg-kicker text-white/55">The clear path to operating</div><h1 className="mt-4 font-display text-5xl font-bold leading-[1.03] tracking-tight">Progress that<br /><span className="text-[hsl(var(--accent))]">moves with you.</span></h1><p className="mt-6 max-w-md text-base leading-relaxed text-white/65">Seva bot brings every approval, document and deadline into one calm control center for high-stakes work.</p><div className="mt-10 flex items-center gap-5 text-xs text-white/55"><span className="flex items-center gap-2"><ShieldCheck size={15} className="text-[hsl(var(--accent))]" />Trusted workflows</span><span className="flex items-center gap-2"><LockKeyhole size={15} className="text-[hsl(var(--accent))]" />Secure by design</span></div></div><div className="relative flex justify-between text-[10px] text-white/35"><span>© 2025 Seva bot</span><span>Tamil Nadu · India</span></div></div><div className="flex items-center justify-center bg-[hsl(var(--background))] p-5 sm:p-10"><div className="w-full max-w-[440px]"><div className="mb-10 lg:hidden"><Logo /></div><div className="mb-8"><div className="cg-kicker mb-3">Welcome back</div><h2 className="font-display text-3xl font-bold tracking-tight">{mode === 'login' ? 'Sign in to Seva bot' : 'Create your workspace'}</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">{mode === 'login' ? 'Continue where your approvals left off.' : 'Set up a clearer path for your next project.'}</p></div><div className="mb-6 grid grid-cols-2 gap-2 rounded-xl bg-[hsl(var(--muted))] p-1"><button onClick={() => setPath('applicant')} data-testid="button-path-applicant" className={cn('rounded-lg py-2 text-xs font-semibold', path === 'applicant' ? 'bg-[hsl(var(--card))] shadow-sm' : 'text-[hsl(var(--muted-foreground))]')}>Applicant / founder</button><button onClick={() => setPath('officer')} data-testid="button-path-officer" className={cn('rounded-lg py-2 text-xs font-semibold', path === 'officer' ? 'bg-[hsl(var(--card))] shadow-sm' : 'text-[hsl(var(--muted-foreground))]')}>Department officer</button></div><form onSubmit={e => { e.preventDefault(); setLocation(path === 'officer' ? '/department' : '/dashboard'); }} className="space-y-4"><label className="block text-xs font-semibold">Work email<input type="email" defaultValue={path === 'officer' ? 'priya.nair@tn.gov.in' : 'arun@greentech.in'} data-testid="input-login-email" className="mt-2 h-11 w-full rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 text-sm outline-none focus:border-[hsl(var(--primary))]" /></label><label className="block text-xs font-semibold">Password<input type="password" defaultValue="password" data-testid="input-login-password" className="mt-2 h-11 w-full rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 text-sm outline-none focus:border-[hsl(var(--primary))]" /></label>{mode === 'login' && <div className="flex justify-end"><button type="button" onClick={() => window.alert('Password reset link requested.')} data-testid="button-forgot-password" className="text-xs font-semibold text-[hsl(var(--primary))]">Forgot password?</button></div>}<Button type="submit" className="h-11 w-full" testId="button-submit-login">{mode === 'login' ? <><LogIn size={16} />Sign in as {path === 'officer' ? 'officer' : 'founder'}</> : 'Create account'}<ArrowRight size={16} /></Button></form><div className="my-7 flex items-center gap-3 text-xs text-[hsl(var(--muted-foreground))]"><span className="h-px flex-1 bg-[hsl(var(--border))]" />or<span className="h-px flex-1 bg-[hsl(var(--border))]" /></div><Button variant="outline" onClick={() => window.alert('Demo access enabled.')} className="w-full" testId="button-demo-access"><Sparkles size={15} />Explore a guided demo</Button><p className="mt-8 text-center text-xs text-[hsl(var(--muted-foreground))]">{mode === 'login' ? 'New to Seva bot?' : 'Already have an account?'} <button onClick={() => setMode(mode === 'login' ? 'register' : 'login')} data-testid="button-toggle-auth" className="font-bold text-[hsl(var(--primary))]">{mode === 'login' ? 'Create an account' : 'Sign in'}</button></p></div></div></div>;
}

function Modal({ title, children, onClose }: { title: string; children: ReactNode; onClose: () => void }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-[hsl(var(--primary)/.3)] p-4" onClick={onClose}><div className="w-full max-w-md rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 shadow-2xl" onClick={e => e.stopPropagation()}><div className="flex items-center justify-between"><h2 className="font-display text-lg font-bold">{title}</h2><button onClick={onClose} data-testid="button-close-modal"><X size={18} /></button></div><div className="mt-5">{children}</div></div></div>; }
function EmptyState({ title, description }: { title: string; description: string }) { return <div className="p-10 text-center"><div className="mx-auto grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--muted))]"><Info size={18} /></div><h3 className="mt-3 font-semibold">{title}</h3><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">{description}</p></div>; }

function RoutedErrorBoundary({ children }: { children: ReactNode }) { const [location] = useLocation(); return <ErrorBoundary resetKey={location}>{children}</ErrorBoundary>; }

function Router() {
  return <RoutedErrorBoundary><Switch>
    <Route path="/login" component={Login} />
    <Route path="/dashboard"><Shell><Dashboard /></Shell></Route>
    <Route path="/projects"><Shell><Projects /></Shell></Route>
    <Route path="/approvals/:id"><Shell><ApprovalDetail /></Shell></Route>
    <Route path="/approvals"><Shell><Approvals /></Shell></Route>
    <Route path="/applications"><Shell><Applications /></Shell></Route>
    <Route path="/documents"><Shell><Documents /></Shell></Route>
    <Route path="/inspections"><Shell><Inspections /></Shell></Route>
    <Route path="/renewals"><Shell><Renewals /></Shell></Route>
    <Route path="/incentives"><Shell><Incentives /></Shell></Route>
    <Route path="/compliance"><Shell><Compliance /></Shell></Route>
    <Route path="/grievances"><Shell><Grievances /></Shell></Route>
    <Route path="/notifications"><Shell><Notifications /></Shell></Route>
    <Route path="/analytics"><Shell><Analytics /></Shell></Route>
    <Route path="/department/applications/:id"><Shell><OfficerReview /></Shell></Route>
    <Route path="/department"><Shell><Department /></Shell></Route>
    <Route path="/settings"><Shell><SettingsPage /></Shell></Route>
    <Route path="/"><Shell><Dashboard /></Shell></Route>
    <Route><Shell><EmptyState title="Page not found" description="The page you requested does not exist." /></Shell></Route>
  </Switch></RoutedErrorBoundary>;
}

function App() {
  return <TooltipProvider><WouterRouter base={import.meta.env.BASE_URL.replace(/\/$/, '')}><Router /></WouterRouter><Toaster /></TooltipProvider>;
}

export default App;